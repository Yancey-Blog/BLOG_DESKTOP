import React, { Component } from 'react';
import cs from 'classnames';
import styles from './apps.module.css';

class Archive extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }

  componentWillMount() {
  }

  componentDidMount() {
  }

  componentWillUnmount() {
  }

  render() {
    return (
      <main className={styles.apps_wrapper} />
    );
  }
}

export default Archive;
